# Continue the Arwa Mizantra Project

Use this context in the new project window:

> We are continuing the dedicated Arwa Egypt Mizantra deployment. The repository is `qutubkothari/arwa-mizantra`, the local folder is `C:\Users\QK\Documents\GitHub\arwa-mizantra`, the public domain is `https://arwa.mizantra.ae`, and the server root is `/var/www/arwa-mizantra`. It has an isolated Supabase database and separate PM2 processes/ports. Never deploy Arwa changes to Saif live or Mizantra test, never reuse their credentials, and never copy their tenant data. Read `docs/ARWA_PROJECT_HANDOVER.md` before making changes.

Do not paste secrets into chat, source files or commits. Use the protected server environment file.
